const raffles=[{title:'Community Learning Lab Fund',org:'Mahlasedi Secondary School',province:'Gauteng',price:'R 50',sold:82,status:'Active'},{title:'Junior Rugby Tour',org:'West Coast Youth Club',province:'Western Cape',price:'R 30',sold:64,status:'Active'},{title:'Mobile Library Project',org:'Read Forward Foundation',province:'KwaZulu-Natal',price:'R 20',sold:91,status:'Closing soon'}];const grid=document.querySelector('#raffle-grid');const empty=document.querySelector('#empty-state');function render(items){if(!grid)return;grid.innerHTML=items.map((r,i)=>`<article class="raffle-card"><div class="image">${r.province}</div><div class="meta"><span>${r.org}</span><span class="badge active">${r.status}</span></div><h3>${r.title}</h3><p>Tickets from <strong>${r.price}</strong></p><div class="progress" aria-label="${r.sold} percent sold"><span style="width:${r.sold}%"></span></div><div class="meta"><span>${r.sold}% sold</span><a href="./raffle.html?id=${i+1}">View raffle</a></div></article>`).join('');empty.hidden=items.length>0}render(raffles);document.querySelector('#raffle-search')?.addEventListener('input',e=>{const q=e.target.value.toLowerCase();render(raffles.filter(r=>Object.values(r).join(' ').toLowerCase().includes(q)))});const toggle=document.querySelector('.menu-toggle');const nav=document.querySelector('.nav');toggle?.addEventListener('click',()=>{const open=nav.classList.toggle('open');toggle.setAttribute('aria-expanded',String(open))});document.querySelectorAll('[data-demo-form]').forEach(form=>form.addEventListener('submit',e=>{e.preventDefault();const message=form.querySelector('[data-form-message]');if(message){message.textContent='Demo mode: authentication and payments require a production backend configuration.';message.hidden=false}}));
